{
  "name": "yt-downloader",
  "version": "1.0.0",
  "description": "Simple YouTube downloader with yt-dlp metadata and streaming",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "echo \"No build step required\"",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "yt-dlp-exec": "^1.0.2"
  }
}
